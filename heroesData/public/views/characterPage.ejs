<%- include('./partials/header.ejs') %>
<body class="bg-gray-100 flex flex-col min-h-screen">
  <%- include('./partials/nav.ejs') %>

  <main class="flex justify-center items-center flex-grow">
    <div class="w-full max-w-md bg-white rounded-lg shadow-lg overflow-hidden">
      <div class="flex justify-center p-4">
        <img src="<%= character.imageUrl %>" alt="<%= character.name %>" class="w-full h-full object-cover rounded-lg" style="max-width: 500px; max-height: 500px">
      </div>
      <div class="p-6">
        <h1 class="text-2xl font-bold text-center mb-4"><%= character.name %></h1>
        <p class="text-gray-700 text-center mb-6"><%= character.description %></p>
        <div class="overflow-x-auto">
          <table class="min-w-full bg-white">
            <tbody>
              <tr>
                <td class="border px-4 py-2 font-semibold">Age:</td>
                <td class="border px-4 py-2"><%= character.age %></td>
              </tr>
              <tr>
                <td class="border px-4 py-2 font-semibold">Active:</td>
                <td class="border px-4 py-2"><%= character.isActive ? 'Yes' : 'No' %></td>
              </tr>
              <tr>
                <td class="border px-4 py-2 font-semibold">Birth Date:</td>
                <td class="border px-4 py-2"><%= character.birthDate %></td>
              </tr>
              <tr>
                <td class="border px-4 py-2 font-semibold">Role:</td>
                <td class="border px-4 py-2"><%= character.role %></td>
              </tr>
              <tr>
                <td class="border px-4 py-2 font-semibold">Hobbies:</td>
                <td class="border px-4 py-2"><%= character.hobbies.join(', ') %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </main>

  <%- include('./partials/footer.ejs') %>
</body>
</html>